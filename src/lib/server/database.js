/* import postgres from 'postgres'
import { PG_HOST, PG_PORT, PG_DB, PG_USER, PG_PASS } from '$env/static/private';

const sql = postgres({
  host                 : PG_HOST,            // Postgres ip address[s] or domain name[s]
  port                 : PG_PORT,          // Postgres server port[s]
  database             : PG_DB,            // Name of database to connect to
  username             : PG_USER,            // Username of database user
  password             : PG_PASS,            // Password of database user
})

export default sql */